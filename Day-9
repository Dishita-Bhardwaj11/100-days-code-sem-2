#include <stdio.h>
#include <string.h>
int main() {
    char s[100001];
    scanf("%s", s);

    int l = 0, r = strlen(s) - 1;
    while (l < r) {
        char temp = s[l];
        s[l] = s[r];
        s[r] = temp;
        l++;
        r--;
    }
    printf("%s", s);
    return 0;
}
